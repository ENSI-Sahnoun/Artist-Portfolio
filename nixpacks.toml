[phases.setup]
nixPkgs = ["php", "phpPackages.composer", "curl"]

[phases.install]
cmds = ["composer install --no-dev --no-interaction 2>/dev/null || true"]

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t . router.php"
